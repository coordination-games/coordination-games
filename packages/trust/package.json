{
  "name": "@coordination-games/trust",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --skipLibCheck",
    "test": "vitest run",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "sample:binding": "tsx src/sample.ts",
    "sample:attestation": "tsx src/attestation-sample.ts"
  },
  "dependencies": {
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
